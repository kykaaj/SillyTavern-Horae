import { readFileSync, writeFileSync } from 'fs';
let s = readFileSync('index.js', 'utf-8');

s = s.replace(
  '  ❌ 错误示范：\"U和艾伦在酒馆聊天，两人聊得很开心，最后约定下次再见。\"（毫无细节）',
  '  ❌ Неправильно: \"{{user}} и Аллен болтали в таверне, им было весело...\"'
);
s = s.replace(
  '  ✅ 正确示范：\"U在酒馆向艾伦打听黑市商人的下落，艾伦起初警惕，但在U递出10枚金币后，透露商人明晚会在废弃码头出现。艾伦对U的态度由戒备转为贪婪。两人约定明晚一起行动。\"',
  '  ✅ Правильно: \"{{user}} в таверне расспрашивал Аллена о торговце на чёрном рынке...\"'
);

writeFileSync('index.js', s, 'utf-8');
console.log('Fixed!');
